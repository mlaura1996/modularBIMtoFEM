# Moved

This methodology document has been superseded by the structured
documentation site under `docs/source/`, so the material-characterisation
content stays in one place instead of two. See:

- `docs/source/developer_guide/materials_hmo_mqi.md` — the reusable
  `hmo_mqi.py` code, the MQI method, the formulas.
- `docs/source/case_study/materials.md` — the Castelnuovo-specific
  classification, inputs, and results.

Build the site with `sphinx-build -b html docs/source docs/build/html`
(see `docs/requirements.txt` for build dependencies), or read the sources
directly as Markdown.
