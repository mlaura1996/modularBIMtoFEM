# User Guide

Practical instructions to reproduce a run: install the pipeline, build the
Docker image, run a mesh, run an analysis, retrieve results. For *why* the
pipeline is built this way, see the {doc}`../developer_guide/index`; for the
Castelnuovo-specific numbers, see the {doc}`../case_study/index`.

```{toctree}
:maxdepth: 2

installation
docker_image
running_the_pipeline
```
