# Framework for Developer

How the codebase is organised and why, for anyone extending the pipeline.
Each task (A/B/C — interface detection, parallel export, the Docker image)
gets its own page covering the design decisions and the real problems hit
while building it, not just the final API.

```{toctree}
:maxdepth: 2

architecture
interface_detection
parallel_export
docker_image_build
materials_hmo_mqi
known_issues
```
